---
description: Add, commit with caveman-style message, and push to main
---

1. Run `git add -A`
2. Generate a commit message following the same style as /caveman-commit (short, terse, present-tense, no fluff)
3. Run `git commit -a -m "<generated message>"`
4. Run `git push origin main`

Show the diff/status before committing so I can review, then proceed.